[wheel]
universal = 1

[pycodestyle]
max-line-length = 80
exclude = docs/*,build/*,venv/*

[pydocstyle]
ignore = D203,D212
match = '(?!docs|build|venv).*\.py'

[tool:pytest]
addopts = --max-slave-restart=0 --showlocals --verbose  --cov pytest_rabbitmq --cov tests tests
testpaths = tests
